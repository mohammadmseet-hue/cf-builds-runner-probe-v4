{
  "name": "cf-builds-runner-probe-v4",
  "version": "1.0.0",
  "description": "Workers Builds CI runner environment probe",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260520.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.0.0"
  }
}
